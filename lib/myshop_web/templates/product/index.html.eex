<h1>Listing Products</h1>

<table>
  <thead>
    <tr>
      <th>Category</th>
      <th>Name</th>
      <th>Url</th>
      <th>Notes</th>
      <th>Brand</th>
      <th>Description</th>
      <th>Buy price</th>
      <th>Sell price</th>

      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for product <- @products do %>
    <tr>
      <td><%= product.category.name %></td>
      <td><%= product.name %></td>
      <td><%= product.url %></td>
      <td><%= product.notes %></td>
      <td><%= product.brand %></td>
      <td><%= product.description %></td>
      <td><%= product.buy_price %></td>
      <td><%= product.sell_price %></td>

      <td><%= link "Show", to: Routes.product_path(@conn, :show, product) %></td>
      <td><%= link "Edit", to: Routes.product_path(@conn, :edit, product) %></td>
      <td><%= link "Delete", to: Routes.product_path(@conn, :delete, product), method: :delete, data: [confirm: "Are you sure?"] %></td>
    </tr>
<% end %>
  </tbody>
</table>

<span><%= link "New Product", to: Routes.product_path(@conn, :new) %></span>
